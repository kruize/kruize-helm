apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kruize.fullname" . }}-test-connection"
  labels:
    {{- include "kruize.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kruize.fullname" . }}:{{ .Values.kruize.service.port }}']
  restartPolicy: Never

